# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload', grace_period: 1 do
  watch(%r{_root})
  watch(%r{partials})
  watch(%r{artisteer})
  watch(%r{data.yml})
end

